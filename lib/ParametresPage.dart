import 'package:flutter/material.dart';

void main() {
  runApp(ParametresPage());
}
class ParametresPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Parametres Page'),
      )


    );

  }
}